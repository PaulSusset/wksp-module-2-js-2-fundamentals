// Q1
// Write a function that accepts two integers and displays
// the greater one.

// Define the function
function max(num1, num2) {
    a = Math.max(num1, num2);
    console.log(a);
}
// Call the function
max(12, 43);